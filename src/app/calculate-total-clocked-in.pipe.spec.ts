import { CalculateTotalClockedInPipe } from './calculate-total-clocked-in.pipe';

describe('CalculateTotalClockedInPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateTotalClockedInPipe();
    expect(pipe).toBeTruthy();
  });
});
